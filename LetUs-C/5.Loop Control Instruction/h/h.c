/*h.Wite a program to receive an integer and find its octal equivalent 
(Note: To obtain octal equivalent of an integer, divide it continuously by 8 till dividend doesn't becomes zero,
then write the remainders obtained in reverse direction)*/

#include<stdio.h>

int main()
{
    int num;

    printf("\nEnter a number to find its opctal value :\t");
    scanf("%d", &num);

    while()
    {
        num 
    }

     return(0);   

}