/*e.A positive integer is entered through the keyboard.
Write a function to obtain the prime factors of this number.*/

#include <stdio.h>

int prime_factor(int);

int main()
{
    int num;

    printf("\nEnter a number to find its prime factor :\t");
    scanf("%d", &num);

    return(0);

}